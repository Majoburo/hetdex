Things left to do:

-Calculate area of partial pixels (idea: cut the pixels in smalles pixels till our resolution met criteria)
-Create a SQL query for SDSSIII data (Using SDSSII data now but quality of image is one pixel per ifu...)
